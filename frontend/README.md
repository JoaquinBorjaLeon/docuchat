# DocuChat Frontend

React chat interface for the DocuChat RAG API.

## Setup

```bash
cd frontend
npm install
npm run dev
```

The dev server starts at `http://localhost:5173`.

Make sure the backend is running on port 8000:

```bash
uvicorn src.api:app --reload
```
